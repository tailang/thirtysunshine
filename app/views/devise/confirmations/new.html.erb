<%= provide(:title, "重新发送验证邮箱") %>
<div class="row">
  <div class="span9">
    <div class="box">
     <span id="existing-account">重新发送验证邮件</span>
     <%= form_for(resource, :as => resource_name, :url => confirmation_path(resource_name), :html => { :method => :post, :class => "form-horizontal" }) do |f| %>
     <%= render 'shared/error_messages', object: f.object %>
     <div class="control-group">
       <div class="control-label"><%= f.label :email, "邮箱" %></div>
       <div class="controls"> <%= f.email_field :email, :autofocus => true %>
       <p>输入您的注册邮箱，我们将重新发送验证邮件</p>
       </div>
     </div>
  
     <div class="control-group">
	   <div class="controls">
	   	<%= f.submit "发送验证邮件", :class => "btn btn-primary" %>
       </div>     	
     </div>
     <% end %>
    </div>
  </div>
</div>



