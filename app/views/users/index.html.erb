<%= provide(:title, "成员") %>
<div class="box">
   <% @users.each do |user| %>
    <%= link_to image_tag(user.avatar_url(:normal)), user, :data => {:toggle => "tooltip"}, :title => user.username %>
  <% end %>
</div>
 


